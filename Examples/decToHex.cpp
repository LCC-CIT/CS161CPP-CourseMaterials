#include <iostream>
using namespace std;

int main()
{
    string name, first, middle, last;

    cin>> first;
    cin >> middle;
    cin >> last;

    cout << first << " " << middle << " " << last;
    return 0;
}
