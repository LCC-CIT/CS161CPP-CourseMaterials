#include <iostream>
using namespace std;

float calcPower(float base, float exponent);

int main()
{
    float num = 5.0, power = 3.0;
    cout << calcPower(num, power);

    return 0;
}

float calcPower(float base, float exponent)
{
    return 0.0;
}
